language: java

jdk:
  - openjdk7
  - openjdk8
  - oraclejdk8

branches:
  only:
    - master

notifications:
  email: false

script: "mvn cobertura:cobertura"

after_success:
  - mvn coveralls:report
  - bash <(curl -s https://codecov.io/bash)
